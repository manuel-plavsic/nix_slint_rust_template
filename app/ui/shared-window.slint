import { Button, VerticalBox } from "std-widgets.slint";

export component SharedWindow inherits Window {
    VerticalBox {
        Text {
            text: "ExampleApp";
        }

        Text {
            text: "This text is shared across all platforms)";
        }
    }
}
